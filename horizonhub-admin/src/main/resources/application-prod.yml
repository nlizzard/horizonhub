# 生产环境配置覆盖（通过 SPRING_PROFILES_ACTIVE=prod 激活）。
# 敏感项此处不提供默认值：缺失对应环境变量时，Spring 占位符解析失败、应用直接启动失败（fail-fast），
# 杜绝误用 dev 默认弱口令。部署时必须注入：
#   DB_USERNAME / DB_PASSWORD / DB_HOST / DB_NAME
#   REDIS_HOST / REDIS_PASSWORD
#   MAIL_PASSWORD / ADMIN_EMAILS
#   ADMIN_NAME / ADMIN_PASSWORD（后台登录强随机口令）
#   PROJECT_FOLDER
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/${DB_NAME}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    druid:
      # 生产环境关闭 Druid 监控控制台
      stat-view-servlet:
        enabled: false
  data:
    redis:
      host: ${REDIS_HOST}
      password: ${REDIS_PASSWORD}
  mail:
    password: ${MAIL_PASSWORD}
  # Session Cookie 安全加固
  servlet:
    session:
      cookie:
        http-only: true
        secure: true
        same-site: strict

admin:
  emails: ${ADMIN_EMAILS}
  # 生产后台登录账号与强随机口令，由环境变量注入
  adminName: ${ADMIN_NAME}
  password: ${ADMIN_PASSWORD}

projectFolder: ${PROJECT_FOLDER}

send:
  mail:
    open: true

log:
  root:
    level: WARN
